<% provide(:title, "User details") %>
<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Password:</strong>
  <%= @user.password %>
</p>

<p>
  <%= link_to 'Checkout Book', indexForMembers_book_path %>
</p>

<p>

  <h1>Currently Checked Out Books</h1>
    <table cellpadding="10">
      <thead>
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th>Authors</th>
        <!--<th>ISBN</th>-->
        <th>Status</th>
        <th colspan="1"></th>
      </tr>
      </thead>

      <tbody>
      <%@books = Book.search(params[@user.id]) %>
      <% @books.each do |book| %>
          <tr>
            <td><%= book.title %></td>
            <td><%= book.isbn %></td>
            <td><%= book.description %></td>
            <td><%= book.authors %></td>
            <td><%= book.status %></td>
            <td><%= link_to 'Return Book ', return_book_path(book.id), :class => 'btn' %></td>
          </tr>
      <% end %>
      </tbody>
    </table>
    <br>
</p>

<!--<p>-->
<!--<strong>Status:</strong>-->
<!--<%= @user.status %>-->
<!--</p>-->

<!--<p>-->
<!--<strong>Profile:</strong>-->
<!--<%= @user.profile %>-->
<!--</p>-->

<%= link_to 'Edit', edit_user_path(@user) %>
